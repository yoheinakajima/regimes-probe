# Status — grounded claim ledger

Every claim below is tagged with its supporting artifact. Claims without a
committed artifact are listed as *not yet verified* or *to avoid*. This file is
the honesty contract for the project (mirrors the `regimes` discipline).

Last updated for: v0 (synthetic harness, Study 0 + Study 1).

**Readiness: LIVE EXECUTOR WIRED BUT NOT EXECUTED; NOT BENCHMARK-CLAIMED.** The
no-key scaffold is frozen and auditable, **and** the live executor
(`scripts/run_live.py` + `src/regimes_probe/live/`) is now wired into the same
harness — safe by default (dry-run; refuses to spend without `--execute`). **No
provider-calling run has been performed; the first one is still pending.** The
ladder is `docs/LIVE_LADDER.md` (A–F); start with the tiny 10/20, budgets [1, 3].
**Defaults are cheap-first** (`gpt-5.4-mini`, provider-diverse routing); OpenAI
hosted `web_search` + `gpt-5.5` is an opt-in strong baseline (rung F), not the
default experiment. Until a run clears `docs/FIRST_REAL_RESULT_CRITERIA.md` with
`headline_eligible = true`, **no benchmark performance is claimed.**

**One-line summary:** the scaffold demonstrates the intended mechanism on a
synthetic fixture; **no BrowseComp/LiveBrowseComp performance is claimed.** The
skeptic's companion to this ledger is `docs/METHODOLOGY_RISKS.md`; the path to a
real result is `docs/REAL_BENCHMARK_READINESS.md` + `docs/NEXT_LIVE_RUN.md`.

**Three distinct things — do not conflate them:**

1. **Synthetic-harness result** (`results/demo/`, Study 0/1) — numbers on an
   *engineered* fixture. Proves the mechanism + plumbing learn the choices the
   fixture rewards. `headline_eligible = false`. **Not a benchmark.**
2. **Real-data-shaped smoke test** (`tests/test_real_data_shape.py`,
   `fixtures/real_shaped/`) — runs the *real adapter path* (decode → split →
   report → leakage → replay) on *fictional placeholder* data. Proves the
   pipeline accepts real-benchmark *structure*. **No performance meaning at all.**
3. **Future real benchmark result** (LiveBrowseComp / BrowseComp) — not yet run.
   Requires keys + data and must clear `docs/FIRST_REAL_RESULT_CRITERIA.md` with
   `headline_eligible = true`. **The only thing that could be a benchmark claim.**

**⛔ Active blocker — LiveBrowseComp obfuscation.** The HF dataset
`Forival/LiveBrowseComp` has been *downloaded* but its `problem`/`answer` fields
are **encoded, not plaintext**. The adapter now **fails closed** (refuses to pass
encrypted text as a question; `scripts/run_live.py` refuses with no placeholder
fallback when a real path is supplied), so **LiveBrowseComp cannot be executed
until the official decode/plaintext path is resolved** — provide a plaintext
export or the official canary/decode utility (validated). The decode scheme could
not be verified from this environment (network restricted). See
`docs/BENCHMARK_TARGETS.md` and `docs/NEXT_LIVE_RUN.md`.

## ✅ Verified claims (with artifacts)

| claim | evidence |
| --- | --- |
| The harness runs with no API keys and no network. | `python -m pytest -q` → 68 passing; `scripts/*` run offline; `scripts/run_synthetic_full.py` runs the whole pipeline offline. |
| The **real-benchmark adapter path** works on real-data-shaped inputs (no keys). | `tests/test_real_data_shape.py` (BrowseComp decode + LiveBrowseComp JSONL + split + report + leakage + replay on `fixtures/real_shaped/` placeholders). |
| **Four baselines/controls** are distinguished: `closed_book`, `no_memory_search`, `random_memory`, `policy_memory`. | `scripts/run_synthetic_full.py` output + `results/demo/{report.json,budget_curve.csv}`; `tests/test_baselines_and_eligibility.py`. |
| The **closed-book baseline makes no tool calls** and answers only intrinsic knowledge. | `tests/test_baselines_and_eligibility.py::test_closed_book_makes_no_tool_calls`. |
| A **same-conditions validator** enforces baseline/policy differ only in memory access. | `eval/conditions.py`; `tests/test_baselines_and_eligibility.py`. |
| Reports carry **`headline_eligible`** with reasons; synthetic runs are correctly **ineligible**. | `eval/eligibility.py`; `results/demo/report.json` (`headline_eligible: false`, reason = synthetic); `tests/test_baselines_and_eligibility.py`. |
| **Ablation scaffolding** runs no-key (routing_only … online_learning_confirm). | `scripts/run_ablations.py`, `config/default.yaml` `ablations:`; reports under `results/ablations/`. |
| The synthetic fixture exercises a **variety of failure regimes** (not cartoonishly perfect). | `tests/test_failure_regimes.py`; `dominant_regimes` on a baseline shows ≥4 regimes incl. route_miss + verification/stale/contradiction. |
| Policy memory snapshots are **inspectable and answer-free**. | `scripts/inspect_memory_snapshot.py` (leakage scan PASS). |
| Two runs can be **compared** (budget curves, deltas, McNemar). | `scripts/compare_runs.py`. |
| A **first-live-run preflight** validates everything (split, conditions, eligibility, budgets, env-var names, cost) and writes a dry-run manifest **without calling any provider**. | `scripts/preflight_first_live_run.py`; 0 blockers on the synthetic default. |
| **Call/cost is estimable** before any spend; dollar cost stays "unknown" unless prices are configured (no vendor prices hard-coded). | `eval/cost.py`, `scripts/estimate_live_cost.py`; `tests/test_live_readiness_scaffold.py`. |
| A **run manifest** records git/dataset/split/models/prompts/tools/budgets/memory/eligibility/cost with **no secrets** (env-var names only). | `eval/manifest.py`; `results/demo/run_manifest.json`; `tests/test_live_readiness_scaffold.py`. |
| **Prompts are version+hash pinned** and folded into the same-conditions fingerprint. | `agent/prompts.py`; `ConditionSpec.answer_prompt_version`; `tests/test_live_readiness_scaffold.py`. |
| Run artifacts have a **hash ledger**; a **conservative claim generator** REFUSES performance claims when `headline_eligible=false`. | `scripts/hash_artifacts.py` (`results/demo/artifact_hashes.json`), `scripts/generate_claims.py` (`results/demo/claims_candidates.md`). |
| Docs lint + no-overclaim check passes; no-key Make targets run. | `scripts/docs_check.py` (25 docs), `Makefile` (`make check`). |
| The **live executor is wired** into the provider-agnostic harness, **safe by default** (dry-run; `--execute` required to spend). | `scripts/run_live.py`, `src/regimes_probe/live/{providers,cache,runner}.py`; `tests/test_run_live_safety.py`. |
| Live calls are **cache/replay-guarded** (no re-spend) and **un-armed in dry-run** (refuse to call). | `RecordingCache`, `CachedProvider`/`NotArmed`; `tests/test_run_live_safety.py`. |
| The execute pipeline passes **same-conditions + headline-eligibility** plumbing (verified with mock providers, no network). | `tests/test_run_live_safety.py::test_run_live_pipeline_with_mocks_passes_plumbing`. |
| Secrets never enter cache/manifest/artifacts (env-var names only; key-like fields redacted). | `live/cache.sanitize`; `tests/test_run_live_safety.py`. |
| LiveBrowseComp **fails closed** on obfuscated rows — encrypted `problem` is never passed through as a question; `run_live` refuses (no placeholder fallback when a real path is given). | `datasets/livebrowsecomp.py`; `scripts/run_live.py`; `tests/test_livebrowsecomp_failclosed.py` (9 tests). |
| BrowseComp decode matches openai/simple-evals **byte-for-byte** (`derive_key` = repeated `sha256(canary)`). | `datasets/browsecomp.py`; `tests/test_browsecomp_decrypt.py`. |
| Live config is **cheap-first**: answerer/web_search default `gpt-5.4-mini`; OpenAI hosted `web_search` is **opt-in** (modes cheap/diverse/openai-hosted); no silent fallback to expensive hosted search; gpt-5.5+hosted warns. | `tools/openai_web_search.py` (no gpt-5.5 default), `live/settings.py`, `scripts/run_live.py`; `tests/test_live_provider_modes.py` (12 tests). |
| Extended provider arms (Monid agentic-discovery, Firecrawl search/scrape/interact, Wokelo specialized-research) are **optional, never default, and safety-gated**; stateful/paid and browser-like tools are off without explicit flags; browser-use is **deferred**; Wokelo **fails closed** without base URL/path; per-arm metadata (family/cost/stateful/safe_default) is recorded in the manifest. | `tools/{metadata,monid,firecrawl,wokelo}.py`, `live/settings.py`; `tests/test_extended_providers.py` (15 tests); `docs/TOOL_ABSTRACTIONS.md`. |
| Reports separate **`structurally_valid`** from **`headline_eligible_memory_claim`**: a run without `policy_memory` (e.g. a plumbing run) is never a memory headline, the "differ only in memory access" phrase only appears when `policy_memory` ran, and claims generation refuses memory-performance claims when `policy_memory` is missing. Headline also requires all four conditions on a real dataset with CONFIRM ≥ `headline.min_confirm_size` (default 20). | `eval/eligibility.py`, `eval/report.py`, `scripts/generate_claims.py`; `tests/test_eligibility_conditions.py`, `tests/test_baselines_and_eligibility.py`. |
| A **provider/API error during a tool call does not crash the run**: it becomes a recorded failed observation (sanitized, no secrets), the bandit is penalized for that arm, per-tool failure counts are reported, and a high failure rate (> `headline.max_provider_failure_rate`, default 0.2) or an entirely-failed required condition blocks the headline while keeping the run structurally valid. | `tools/base.safe_search`, `live/providers.py`, `activegraph_pack/tools.py`, `eval/{reward,metrics,eligibility,report}.py`; `tests/test_tool_failures.py` (10 tests). |
| The graph is a deterministic projection of the event log (replay passes). | `results/demo/replay_check.md` (`projection_matches: true`); `tests/test_fake_tool_replay.py`. |
| Fake tool calls are replayable; replay detects divergence. | `tests/test_fake_tool_replay.py::test_recording_then_replay_reproduces_responses`, `::test_replay_divergence_is_detected`. |
| The project also runs with the standard library + PyYAML only (no `activegraph`). | `EventLog` fallback in `activegraph_pack/behaviors.py`; verified by blocking the import. |
| OPTIMIZE/CONFIRM splits are deterministic and disjoint. | `tests/test_split.py`. |
| The default router uses no LLM and is deterministic given a snapshot. | `tests/test_router_determinism.py`; `policy/router.py`. |
| Budget caps are strictly binding across modes. | `tests/test_budget_caps.py` (budgets 1/3/5/10). |
| Policy memory stores no final answer text; the frozen snapshot is answer-free. | `tests/test_policy_memory_no_answer_leakage.py`; `assert_no_answer_leakage`. |
| On the synthetic fixture, frozen policy memory improves `correct_per_tool_call` vs `no_memory_search` at every budget, improves accuracy at every budget, and far exceeds the `random_memory` control at budget 1. | `results/demo/budget_curve.csv` (budget 1: no_memory_search 0.000, random_memory 0.286, policy_memory 0.750; budget 3 accuracy: 0.214 / 0.500 / 0.857). |
| The improvement is statistically detectable (paired). | `results/demo/report.json` → McNemar at budget 3: 18 wrong→correct flips, 0 reverse, p ≈ 0.0001; bootstrap CI for policy `correct_per_tool_call` excludes the baseline. |
| The synthetic result is **not** perfect — residual failures remain so diagnostics are meaningful. | `results/demo/summary.md` (policy accuracy 0.857 at budget 3, not 1.0); `per_question.csv` regime labels. |
| On this fixture the learned stop policy does not always stop early, so at large budgets `policy_memory` trades calls for accuracy and `correct_per_tool_call` can trail `random_memory` (recorded honestly; thresholds were not tuned to flatter the number). | `results/demo/budget_curve.csv` (budget 5/10: policy_memory mean_tool_calls = budget; stop fires only when the verification score reaches `stopping.stop_threshold`). |
| The regimes loop detects regimes, proposes bounded updates, gates them, and records a promotion decision. | `tests/test_regimes_gates.py`; `results/regimes-demo/policy_updates.json`. |
| Base model weights are unchanged by construction. | No training/fine-tuning code exists; the only learned state is the bandit/memory. |
| Frozen-memory leakage is checked the same way the inspector checks it, and is layer-separated: only frozen policy memory gates the headline; the raw audit trace may contain gold by design. | `src/regimes_probe/eval/leakage.py` (`leakage_check_details`); `results/demo/report.json` → `leakage_check_details`; `tests/test_debug_artifacts.py::test_report_leakage_matches_inspector_for_clean_snapshot`, `::test_raw_archive_gold_does_not_fail_policy_memory_leakage`. |
| `report.json` carries non-empty per-condition metrics plus provider-failure accounting. | `results/demo/report.json` → `metrics`, `provider_failure_rate`, `tool_failures`; `tests/test_debug_artifacts.py::test_report_metrics_populated`, `::test_provider_failures_in_report_and_summary`. |
| Each run emits a bounded, secret-free `debug_questions.jsonl` and two offline triage scripts read it. | `results/demo/debug_questions.jsonl`; `scripts/debug_run_failures.py`, `scripts/summarize_provider_returns.py`; `tests/test_debug_artifacts.py::test_debug_jsonl_bounded_and_has_previews`, `::test_debug_scripts_run`. |
| Every run exports a compact, secret-free, answer-free typed graph projection with all 17 object + 14 relation types, provenance-linked to the event-log replay. | `results/demo/graph_projection.json`; `eval/projection.py`; `tests/test_graph_native.py::test_graph_projection_has_expected_object_and_relation_types`, `::test_projection_is_bounded_to_detail_sample`. |
| The two eligibility verdicts are one flat first-class `eligibility_verdict` object, mirrored in `report.json` and the projection and consumed by the claims generator. | `results/demo/report.json` → `eligibility_verdict`; `eval/eligibility.py:build_eligibility_verdict`; `tests/test_graph_native.py::test_eligibility_verdict_object_in_report_and_projection`, `::test_plumbing_run_structurally_valid_but_not_headline`, `::test_all_four_real_can_be_headline_eligible`, `::test_synthetic_cannot_be_headline_eligible`. |
| Failures are first-class `failure_regime` objects attached to attempts (provider failures become `provider_error`, not strings). | `eval/failure_regime.py`; `results/demo/report.json` → `failure_regime_summary`; `tests/test_graph_native.py::test_failure_regime_objects_attach_to_attempts`. |
| Policy fragments carry trace lineage (source trace/attempt ids, top arms) and stay answer-free. | `results/demo/memory_snapshot.json` fragments; `policy/consolidation.py`; `scripts/inspect_memory_snapshot.py`; `tests/test_graph_native.py::test_policy_fragments_link_to_source_traces_without_answers`. |
| Offline forked ablations rerun policy variants on cached outcomes and refuse to call providers; they are never headline-eligible. | `live/fork.py`, `scripts/fork_offline_ablation.py`, `docs/OFFLINE_FORK_ABLATIONS.md`; `tests/test_graph_native.py::test_offline_fork_refuses_live_calls`, `::test_offline_fork_missing_cache_refuses`, `::test_offline_fork_end_to_end_not_headline_no_spend`. |
| `page_fetch` is a follow-up (URL) tool, never a first-hop search arm: the router routes only over search-family tools, page_fetch runs only on a URL from evidence, and a non-URL call fails gracefully with `requires_url`. | `tools/metadata.py` (`FOLLOWUP_FAMILIES`, `is_first_hop`); `agent/search_loop.py` (first-hop/follow-up split); `tools/page_fetch.py`; `tests/test_page_fetch_routing.py` (6 tests incl. provider-failure-rate falls when page_fetch is excluded from first-hop). |
| Level 2 query decomposition (v1) extracts clue **spans** (not single capitalized tokens), composes 3–6 targeted length-capped queries clause-by-clause, scores query quality and drops low-quality candidates; the bandit learns the query form (`tool × query_arm`) and the long full-question query is never the cold-start default. | `policy/query_decomposition.py` (`extract_clues`/`decompose`/`score_query`); `policy/query_policy.py` (`--enable-query-decomposition`); `tests/test_query_decomposition.py` (African/Mexican/monument/manga examples → clue spans not `African One`/`Mexican`/`Name December 2023`; quality scoring drops single-generic queries). |
| Benchmark-contaminated results (eval-host / question-mirroring pages) are detected, penalized in reward, and reported. | `eval/contamination.py`; `eval/reward.py` (`contamination_penalty`); `results/{run}/report.json` → `contamination` (count, rate, by provider/domain); `tests/test_query_decomposition.py::test_contamination_*`. |
| Optional iterative clue resolution: staged search extracts candidate entities from results (generic hubs filtered) and searches the top entity with the next clue span / answer-shape hint, under budget; follow-up forms are bandit arms so memory learns whether iterative helps a cluster. | `agent/clue_resolution.py`; `agent/search_loop.py` (staging); `policy/query_policy.py` (`--enable-iterative-clue-resolution`); stage chain in `debug_questions.jsonl` + `scripts/debug_run_failures.py`; `tests/test_iterative_clue_resolution.py` (entity follow-up, generic filtered, budget respected, stage chain, metrics). |
| v1/v2 decomposition reduced contamination but did not solve `exact_answer_missing`; BrowseComp needs iterative clue resolution. Scrape/fetch is to be evaluated only after candidate-entity targeting works. | `docs/QUERY_POLICY.md` (Level 2 / 2b); `docs/EVALUATION_PROTOCOL.md` (query-formulation ladder). |
| Candidate-hypothesis policy: extracted candidates are typed by generic role; the target role is inferred from the question; role-incompatible candidates (sources/broad orgs/broad locations/concepts) are penalized; a candidate is carried forward only with evidence progress; an anti-sticky beam forces exploration after repeated no-progress (no `Art Deco → Art Deco → Art Deco` loops). | `agent/clue_resolution.py` (`classify_entity_role`/`infer_target_roles`/`build_hypotheses`/`HypothesisBeam`); `tests/test_candidate_hypothesis.py` (12: role typing, source/location/concept penalized, person>source, title>location, sticky downweight, forced exploration, follow-up = candidate + unresolved clue, memory answer-free). Metrics: `candidate_role_match_rate`, `sticky_candidate_count`, `no_progress_followup_count`, `evidence_improved_after_candidate_rate`, `candidate_switch_count`, `repeated_candidate_query_count`, `mean_beam_size`. |
| Iterative v0 over-selected wrong intermediate candidates; role typing + evidence-progress gating is a generic multi-step-search policy (not BrowseComp-specific); scrape comes after candidate targeting. | `docs/QUERY_POLICY.md` (Level 2c); `docs/EVALUATION_PROTOCOL.md`; `docs/METHODOLOGY_RISKS.md` (sticky wrong-candidate exploitation). |
| Level 3 evidence reading: `firecrawl_scrape` is a URL-only follow-up reading tool (never a first-hop search arm), chosen by a reading policy (scrape for PDF/structured/authoritative-thin pages, page_fetch for simple/fallback); reads count against budget, are cached/replayable, skip contaminated/social/no-progress/no-clue URLs, and fail closed to page_fetch on Firecrawl 402/quota/HTTP errors. | `tools/firecrawl.py` (`FirecrawlScrape`); `agent/reading_policy.py`; `agent/search_loop.py` (read stage); `tests/test_firecrawl_scrape.py` (11: never first-hop, URLs-only, budget, contaminated/social/no-progress skip, 402→failed+fallback). Metrics: `scrape_call_count`, `scrape_success_rate`, `evidence_added_by_scrape_rate`, `scrape_failure_counts`, `scrape_fallback_count`, `scrape_to_answer_rate`. |
| Level 4 task-frame / constraint-graph layer: a question is parsed into a generic, gold-free `TaskFrame` (typed target + latent slots, typed constraints, dependency edges, known-context terms), and a `HypothesisTable` + evidence ledger + action planner drive search/candidate-selection/reading/stopping over it. Target role comes from the interrogative head noun (so secondary entities are intermediates, not answers); known-context terms (e.g. WHO) are never promoted to a target slot; candidate role typing is context-aware ("Casa Verde restaurant" → org); reads fire only with a frame-grounded reason; hypotheses are downweighted after repeated no-progress. Generic (no per-item rules), gated behind `policy.enable_task_frame` (off → synthetic demo byte-identical), and policy memory stays answer-free. | `agent/task_frame.py`; `agent/hypothesis_table.py`; `agent/action_planner.py`; `agent/search_loop.py` (task-frame branch); `tests/test_task_frame.py` (12: restaurant slots/constraints, WHO known-context-not-target, TV person-intermediate+series-target, manga title target, strict context role typing, read-value gate accept/reject/contaminated, hypothesis advance, no-progress downweight, end-to-end loop+debug artifact, answer-free policy memory). Metrics: `slot_resolution_rate`, `constraint_support_rate`, `target_slot_support_rate`, `hypothesis_coverage_score`, `evidence_progress_per_action`, `read_value_precision`, `no_progress_action_rate`, `repeated_equivalent_query_rate`, `hypothesis_rejection_counts`, `final_answer_supported_by_constraints_rate`. |
| Deterministic TaskFrame v0 proved the architecture but **parse quality is the limiting factor** (wrong target, wrong constraint attachment, source/org promoted to target → still 0 accuracy on a structurally-successful live run). Optional cached/validated LLM task-frame parser emits the **same** `TaskFrame` schema (no new planner contract), is **answer-free** (prompt forbids answering; receives only the question; nothing written to policy memory), **schema-validated** (≥1 target; target role matches interrogative head; constraints attach to real slots; no known-context-as-target; no gold-like answer text; no unsupported labels), **cached + replayable** (keyed by prompt/model/question; replay never calls a model), and **fallback-safe** (invalid/low-quality → deterministic parser, `fallback_reason` recorded). Off by default; `--enable-llm-task-frame-parser` requires `--enable-task-frame`. | `agent/llm_task_frame.py` (`LLMTaskFrameParser`/`ParserCache`/`validate_payload`/`score_parse_quality`/`build_task_frame`); `agent/prompts.py` (`task_frame_parser` pinned prompt); `tests/test_llm_task_frame.py` (15, no model calls: accept, 5 fixture question types, invalid-JSON/missing-target/bad-ref/low-quality fallback, known-context & gold-like rejection, cache avoids 2nd call, replay-only + file-cache replay without model, prompt version/hash recorded, deterministic default when off, end-to-end loop+debug provenance, invalid→deterministic). Metrics: `llm_task_frame_used_count`, `llm_task_frame_fallback_count`, `task_frame_parse_quality_mean`, `parser_validation_failure_counts`, `deterministic_fallback_rate`, `target_slot_role_distribution`, `constraint_attachment_count`. |
| LLM task-frame parser is wired to a live model client through the answerer's `RecordingCache` (defaults to `--answer-model`; `--task-frame-parser-model` overrides). Dry-run and replay **never call the model**; a model failure falls back to deterministic (`fallback_reason=model_error:*`). `--enable-llm-task-frame-parser` without `--enable-task-frame` is a **hard configuration error** (non-zero exit, no artifacts written) — flags are never silently downgraded. Parser spend is reported answer-free. | `live/providers.py` (`build_task_frame_model_fn`); `scripts/run_live.py` (`--task-frame-parser-model`, strict validation, model_fn injection); `scripts/_common.py` (`validate_task_frame_flags`, `build_agent`); `live/runner.py`/`eval/report.py`/`eval/manifest.py` (accounting). Accounting: `parser_model_calls_estimated`, `parser_model_calls`, `parser_cache_hits`, `parser_cache_misses`, `parser_fallback_count`, `task_frame_parser_model`. `tests/test_task_frame_gate_and_wiring.py` (CLI guardrail exits nonzero + writes no artifacts; dry-run/replay raise without calling model; parser falls back on model failure; record→replay reuses cache). |
| Answer-support gate: `answer_supported`/`final_answer_supported_by_constraints` is True only when a target candidate exists, non-contaminated evidence tied to the selected hypothesis supports the target slot or answer-shape, the target's blocking/most-discriminative constraint is resolved, no bound-slot constraint is contradicted, and ≥2 constraints are supported — never from slot existence alone. Failures itemized in `missing_support_reasons`. Fixes the earlier `target_sup=1.0`/`answer_supported=True`-with-no-answer bug. | `agent/action_planner.py` (`evaluate_answer_support`, `AnswerSupportResult`); `agent/search_loop.py` (`frame_coverage.answer_support_gate`/`missing_support_reasons`); `tests/test_task_frame_gate_and_wiring.py`. |
| Open-world semantic constraints + operational affordances: the parser is no longer bound to a fixed `constraint_type` enum (which over-fit BrowseComp and caused constant fallback). Constraints carry free-form `semantic_label`/`semantic_facets` (preserved verbatim, never rejected) plus a CLOSED set of operational `affordances` (`can_search/verify/read/compare/bind_slot/support_answer/block_answer`) that the planner branches on. Rich labels (`employment_relation`, `authorship + educational background`, `distance_and_temporal_attribute`) are accepted; validation checks operational usability + safety, not vocabulary; raw semantics and derived affordances are kept side by side. Deterministic + LLM parsers enrich uniformly. | `agent/affordances.py` (`COMMON_FACETS`/`AFFORDANCES`/`derive_affordances`/`normalize_facets`); `agent/task_frame.py` (`Constraint` open-world fields, `enrich_constraint`); `agent/llm_task_frame.py` (open-world `validate_payload` + `parser_warnings`); `agent/action_planner.py` (affordance-driven `plan` actions: `search_to_bind_slot`/`search_to_test_constraint`/`compare_candidates`/`answer_if_supported`/`abstain_if_blocked`); `agent/prompts.py` (`task_frame_parser` v2); `tests/test_open_world_task_frame.py` (13: unknown labels/facets accepted, scalar applies_to normalized, malformed ref falls back, affordance derivation, evidence-path gate, projection semantic-constraint + affordance edges, answer-free memory). |
| Epistemic escalation controller: a deterministic front door picks the cheapest sufficient mode per question (`direct_answer_possible`/`simple_lookup`/`decomposed_search`/`iterative_research`/`task_frame_required`) from clause/entity/multi-hop/recency signals, so easy questions skip the heavy parser and BrowseComp-style multi-hop questions escalate (emergently, not hard-coded). Off by default (benchmark uses explicit flags); `--auto-epistemic-mode`/`--force-task-frame`/`--disable-direct-answer`. Decision recorded per attempt. | `agent/epistemic_mode.py` (`decide_epistemic_mode`/`EpistemicModeDecision`); `agent/search_loop.py` (applies/records the decision); `scripts/preview_task_frames.py` (dry-run preview); `tests/test_open_world_task_frame.py` (easy→direct/simple, multi-hop→task_frame, force override). Graph: `semantic_constraint`/`constraint_facet`/`operational_affordance`/`epistemic_mode_decision`/`answer_support_path` objects + `constraint_has_facet`/`constraint_has_affordance`/`hypothesis_assigns_slot`/`answer_supported_by_path`/`unresolved_constraint_blocks_answer`/`epistemic_mode_for_attempt` relations. |
| LLM-local slot-id validation + consistent remapping: the first open-world live preview showed the parser produced a semantically GOOD frame, but ingestion rejected it — raw slot ids (`T1`/`T2`/`I1`...) and **dict-form `dependency_edges`** (`{"from":"I1","to":"T1"}`) tripped a list-only edge check (repeated `dependency_edge_refs_unknown_slot`). Fixed: references are validated against the **raw** slot-id namespace, edges are parsed flexibly (list OR object forms), then all ids (`slot_id`/`depends_on`/`applies_to`/`supports_answer_slot_ids`/`dependency_edges`) are **remapped consistently** to an internal `s0..sN` namespace with `raw_slot_id` + `id_mapping` recorded. `dependency_edges` are derived from per-slot `depends_on` when omitted; an explicit-vs-`depends_on` disagreement is a warning (prefer `depends_on`), not a fallback; a genuinely unknown ref still falls back. | `agent/llm_task_frame.py` (`_parse_edge`/`_raw_slot_ids`/`_derive_raw_edges`, remap in `_frame_from_payload`, robust edge validation); `agent/task_frame.py` (`Slot.raw_slot_id`, `TaskFrame.id_mapping`); `scripts/preview_task_frames.py` (raw ids / id_mapping / edges before+after / fallback_stage); `tests/test_task_frame_id_remap.py` (9: raw T*/I* accepted, depends_on not an edge error, applies_to + supports remapped, omitted edges derived, inconsistent→warning, unknown ref→fallback, dict-edge unknown endpoint→fallback, preview shows id_mapping). |
| Variable/constant/binding distinction in target-slot validation: the open-world parser produced a semantically GOOD TV-series frame (target "90s TV series", role title_or_work, actor clues as constraints) but it fell back on `known_context_promoted_to_target` because the slot name overlapped the question. Root cause: the validator conflated a variable **descriptor** with a bound **value**. Fixed: slots carry a binding lifecycle (`slot_status`: unbound_variable/known_constant/candidate_binding/derived_value) + `descriptor_text`/`bound_value`/`known_context_refs`/`evidence_required_to_bind`/`parser_confidence`; `classify_target_slot` rejects only a premature binding (bound_value set / known_constant) or a concrete known constant (Title-Case named entity whose role mismatches the interrogative head AND has no binding constraints), accepts descriptor variables (type/relational language, role matches head, or has constraints), and warns (never falls back) on ambiguous cases. "90s TV series"/"founder full name"/"person who wrote the introduction" pass; "World Health Organisation"(asked person)/"Tennessee"(asked series)/"New Mexico"(asked founder)/premature bound_value fail. Reusing question wording in a target descriptor is not leakage. Answer-support gate unchanged (still evidence-based). | `agent/task_frame.py` (`SLOT_STATUSES`, `Slot` variable fields); `agent/llm_task_frame.py` (`_is_named_entity`/`_is_descriptor`/`infer_slot_status`/`classify_target_slot`; rewritten validation); `agent/prompts.py` (`task_frame_parser` v3 variables-vs-constants block); `scripts/preview_task_frames.py` (per-slot status + `known_context_target_check`); graph `known_context_term`/`slot_variable`/`candidate_binding`/`slot_descriptor`/`binding_status` objects + `slot_has_descriptor`/`slot_bound_by_candidate`/`slot_depends_on_context`/`known_context_not_answer`/`target_slot_unbound_until_evidence` relations; `tests/test_task_frame_variable_constant.py` (15: descriptors pass, constants/premature-binding fail, ambiguous warns, end-to-end accept/fallback, frame variable semantics, graph vocabulary, answer-free memory, preview shows slot_status+check). |
| ActiveGraph-native candidate-slate / frontier layer (Level 5): multi-hop search maintains a **candidate slate per unresolved slot** (possible hotels/museums/founders/years), not one global best candidate. Generic (no fixed slot/constraint names, no gold), event-sourced (candidate/status/merge/promotion/hypothesis/frontier events with deterministic ids → projected to graph_projection, replayable/forkable), and **skipped** for direct/simple epistemic modes. Per-slot `SlotCandidate` assignments carry provenance + per-slot constraint support/contradiction + status lifecycle (active/rejected/confirmed/merged/stale); known constants admitted only when evidence binds them to a role-compatible slot; candidates rejected on constraint contradiction (e.g. opened-1955 vs 1972), confirmed when required blocking constraints supported by non-contaminated evidence; duplicates merged preserving provenance. Hypotheses are slot→candidate combinations scored by coverage + source diversity − contradictions/no-progress/contamination. Frontier scheduler picks the next action by expected information gain net of cost (blocking constraints first, upstream before downstream, cheap verify before read); reads require a candidate+slot+constraint triple. Answer-support gate UNCHANGED. | `agent/candidate_frontier.py` (`CandidateFrontier`/`CandidateSlate`/`SlotCandidate`/`FrontierAction`/`FrontierHypothesis`); `agent/search_loop.py` (gated wiring + trace); `activegraph_pack/events.py` (`SLATE_EVENTS`); `activegraph_pack/objects.py`+`relations.py` (12 objects + 15 relations); `eval/projection.py` (`_project_frontier`); `eval/metrics.py` (`_frontier_metrics`); `eval/harness.py`/`eval/debug.py`/`scripts/debug_run_failures.py`; `tests/test_candidate_frontier.py` (15: slate-per-slot, restaurant 5 slates, TV separate slates, active-while-unresolved, contradiction reject, expand-on-confirm, known-context-not-candidate, scheduler blocking>read, read triple, duplicate merge, rejection/promotion in projection, deterministic replay, easy skip, answer-free memory). Metrics: candidate_slate_size_mean, active/confirmed/rejected_candidates_per_slot, candidate_promotion/rejection/merge_rate, hypothesis_branching_factor, frontier_action_counts, frontier_expected_gain_mean, read_on_candidate_rate, answer_from_confirmed_hypothesis_rate, skipped_candidate_slate_count + reason counts. |
| LLM frontier-action / query proposer (Level 5c): the controller drives tool calls correctly, but deterministic query *synthesis* is the bottleneck — `browsecomp-frontier-active-001` showed `frontier_controller` active, `tool_calls_from_frontier=12`, `fallback=0`, yet queries were bare descriptors (`founder`/`report`/`publication`/`nickname`/`potential antagonist`/`19th Century monument`) retrieving junk (FOUNDER Definition, Merriam-Webster, Username Generator). Fix: an LLM **proposes** constraint-grounded actions from a bounded ActiveGraph-derived `ResearchStateCard`; deterministic code **validates, scores, selects, executes, records**. ActiveGraph stays source of truth. Two opt-in flags (both require `--enable-task-frame`, default off, skipped for direct/simple modes, dry-run/replay make **zero** model calls): `--enable-llm-frontier-repair` (LLM consulted ONLY when the deterministic query is generic/blocked; keeps the action, swaps the query) and `--enable-llm-frontier-planner` (LLM proposes top-K 1–5 actions). Card (no gold/secrets): slots, known-context, unresolved blocking constraints, slates, hypotheses, evidence, failed/no-progress queries, budget, det recommendation. `validate_proposal` rejects-with-reason: nonexistent slot/constraint/candidate/hypothesis, generic query, duplicate, no anchor, answer-without-supported-hypothesis, disallowed/unavailable tool, exceeds budget, not-connected-to-unresolved. Survivors scored (EIG + constraint/known-context anchor + discriminative + candidate/hyp relevance + novelty − duplicate/cost/no-progress); top one executes, **every tool call links to its proposal** (`lfp_` `frontier_action_id`, `tool_call_from_llm_frontier_proposal`). Cached by `prompt_fingerprint\|model\|card_hash`; answer-free (never reaches policy memory); `mode\|model\|prompt` stamped into `ConditionSpec.llm_frontier_settings` so same-conditions requires identical settings across arms. | `agent/llm_frontier.py` (`ResearchStateCard`/`build_research_state_card`, `FrontierProposal`, `validate_proposal`/`score_proposal`, `LLMFrontierProposer`); `agent/prompts.py` (`frontier_planner` v1); `live/providers.py` (`build_frontier_model_fn`, temp 0); `agent/planner.py`/`search_loop.py` (gated wiring + `lf_steps` trace); `activegraph_pack/events.py` (`LLM_FRONTIER_EVENTS`); `objects.py`+`relations.py` (5 projection objects + 7 relations); `eval/projection.py` (`_project_llm_frontier`); `eval/metrics.py` (`_llm_frontier_metrics`); `eval/conditions.py` (`llm_frontier_settings`); `scripts/_common.py`/`run_live.py`/`config/default.yaml`/`live/settings.py`/`runner.py`/`eval/manifest.py`/`report.py` (flags + guardrail); `scripts/debug_run_failures.py`; `tests/test_llm_frontier.py` (15: generic-query repair grounds it, repair skipped when specific, generic/nonexistent-slot/duplicate/disallowed-tool proposals rejected, replay uses cache w/ zero model calls, dry-run falls back, planner drives + links calls, projection links proposal→tool_call, repair-only-on-generic, easy skip, answer-free memory, requires-task-frame guardrail, same-conditions detects mismatch). Metrics: llm_frontier_model_calls, proposal_accept_rate, proposal_rejection_counts, generic_query_repaired/blocked_count, selected_query_constraint/known_context_anchor_rate, duplicate_query_blocked_count, evidence_progress_by_llm_frontier_action, llm_frontier_vs_deterministic_agreement_rate, llm_frontier_realized_eig. |
| LLM frontier proposal→action→evidence integrity (Level 5c fix): the first repair run (`browsecomp-llm-frontier-repair-001`) confirmed the LLM composes much better grounded queries and cut mean tool calls, but exposed a **critical bug** — the *selected* proposal's slot/constraints were dropped on execution (repair kept the *deterministic* action and only swapped the query), so a proposal for `slot=s2,constraints=[C1]` executed as `slot=s0,constraints=[C4]`; evidence linked to the wrong slot, support stayed zero, and a correct candidate (`Cristina Ortiz`, HAS-GOLD) was never promoted. Fix: (1) `_to_step_plan` builds the executed action from the **proposal's** action_type/slot/candidate/constraints/query/tool/anchors in BOTH modes; the frontier materializes it as a first-class `FrontierAction` (`register_proposal_action`, id `lfp_<pid>`); `CallRecord` carries `llm_frontier_proposal_id`+`frontier_action_id`+slot+constraints+anchors. (2) `check_proposal_action_integrity` gates execution (slot/constraint/query/fa-id match + post-exec evidence-linked-to-selected-slot/constraints); any mismatch records `frontier_action_integrity_error` and **refuses→falls back to deterministic**. (3) DIRECTED `ingest_evidence(directed_slot_id/constraint_ids/proposal_id)` binds a role-compatible candidate to the SELECTED slot (`_role_compatible`: person→free-form `graphic_designer` ok, date↛person) + links the SELECTED constraints (`evidence_linked_to_llm_proposal`); retrieval-noise candidates (`generic_definition_noise`/`source_platform_noise`/`ui_navigation_noise`) are dropped. (4) Repair triggers broadened beyond one-word generics (`repair_trigger_reason`): repeats-zero-progress, stale/no-progress/slot-incompatible candidate, noise candidate, missing high-priority-constraint anchor, no-supported-constraints-after-N. (5) Honest `progress_components` (raw vs slot_compatible vs selected_slot candidate counts, selected_constraint support, hyp delta, noise count); LLM exec-success requires progress on the SELECTED slot/constraint, not arbitrary candidates. (6) `normalize_tool` accepts enabled concrete tools (`serper_search`/`exa_search`/…), resolves families, rejects only non-enabled — fixes spurious `disallowed_tool:serper_search`. | `agent/candidate_frontier.py` (`register_proposal_action`, directed `ingest_evidence`/`_assign`, `_role_compatible`/`_noise_kind`, `StepPlan` proposal fields + `action_info`, `progress_components`); `agent/llm_frontier.py` (`normalize_tool`, `repair_trigger_reason`, `check_proposal_action_integrity`, rewritten `_to_step_plan`/`plan_step`, new counters); `agent/search_loop.py` (directed ingest, progress-aware exec-success, post-exec evidence integrity, `CallRecord.llm_frontier_proposal_id`); `activegraph_pack/events.py` (7 integrity/linkage events) + `relations.py` (`evidence_linked_to_llm_proposal`/`candidate_promoted_from_llm_frontier`); `eval/projection.py` (selection node audit fields + 2 relations); `eval/metrics.py` (integrity/progress/trigger/tool metrics); `eval/debug.py`/`scripts/debug_run_failures.py`; `tests/test_llm_frontier.py` (12 new: slot/constraint propagation, proposal+fa ids on call, evidence→selected slot/constraints, integrity mismatch→error+fallback, Cristina Ortiz promoted to person slot, progress ignores noise/unrelated, repair on repeated-zero-progress + stale candidate, serper/exa accepted, family→enabled provider, replay preserves links). Metrics: llm_proposal_to_action_integrity_rate, llm_proposal_slot/constraint_match_rate, evidence_linked_to_selected_constraint_rate, llm_frontier_repair_trigger_reason_counts, llm_frontier_tool_normalization_count, llm_frontier_progress_slot_compatible_candidate_rate, llm_frontier_selected_constraint_support_rate, candidate_promoted_from_llm_frontier_count. |
| Level 5f hardening — read-reliable, candidate-safe, support-aware judging (after the `browsecomp-llm-evidence-judge-smoke-001` wiring smoke test, treated as success not benchmark): keep the judge narrow; make the loop reliable + strict (generic, no benchmark-specific patches). (A) Read reliability: `read_candidate_source` requires a concrete clean URL; a zero-char `page_fetch` is NOT a success — it falls back ONCE to `firecrawl_scrape` on the same URL (mirror of the existing scrape→fetch fallback), accounted via `read_failed_zero_chars_count`/`read_fallback_attempted_count`/`read_fallback_success_count`/`read_failed_after_fallback_count`; `read_requires_url_violation_count` pinned 0. (C/D) Confirm gate: a candidate is `confirmed` only when EVERY blocking constraint is on FULL support (partial never counts), the slot value is not junk, and ≥1 DISCRIMINATIVE blocking constraint is supported when one exists; pinned-0 `confirmed_hypothesis_with_unresolved_blocking_count`/`confirmed_candidate_with_junk_blocking_slot_count`/`blocking_constraint_partial_support_confirmed_count`. (G/K) Post-model contract: full support for an identity slot requires a concrete NAMED candidate whose name/alias is in the QUOTE; generic descriptors ("graphic designer"/"founder"/"Kenyan"/"TV shows") never full; relational constraints need subject AND object anchored; pinned-0 `full_support_without_named_candidate_count`/`full_support_from_generic_descriptor_count`/`relational_support_without_object_anchor_count`/`full_or_partial_support_from_contaminated_source_count`. (H) Judge budget: chrome never reaches the judge (`judge_invoked_on_chrome_count`=0), per-candidate call cap, blocking-contradiction early-stop (`contradiction_early_stop_count`/`judge_calls_saved_by_contradiction_stop`). (I) Support-aware: a candidate with clean support is exempt from `repeated_no_progress` (`supported_candidate_rejected_no_progress_count`=0). Deferred (documented): finer read-event taxonomy, exa/firecrawl_search alt-URL fallback, standalone source-subject extraction, support_invalidated lifecycle. Strict answer gate, contamination safety, replay, policy-memory hygiene UNCHANGED. | `agent/candidate_frontier.py` (`_confirmable` all-blocking+discriminative+non-junk, `_slot_blocking_constraints`, support-exempt no-progress, confirm-gate invariant metrics); `agent/evidence_judge.py` (`enforce_hard_rules` named/generic/quote-anchor/relational guards, `_is_named_entity`, judge-budget cap + contradiction-stop counters, guard invariants in `stats`); `agent/evidence_interpreter.py` (`_is_blocking`/`_object_anchored`, per-constraint judge cap + contradiction early-stop + relational/object_anchored pass-through); `agent/search_loop.py` (zero-char read detection + `page_fetch`→`firecrawl_scrape` fallback + `read_acct`); `eval/harness.py`/`eval/metrics.py` (read-accounting + confirm-gate invariant aggregation); `tests/test_evidence_judge_5f.py` (13: confirm needs all-blocking+discriminative, single/partial don't confirm, gate invariants 0, generic-never-full, full-needs-named, quote-must-anchor, relational-needs-object, contaminated-blocked, contradiction-early-stop, supported-not-rejected, unsupported-still-rejected, support_from_read). Metrics: read_failed_zero_chars/fallback_attempted/success/after_fallback, full_support_without_named_candidate_count, full_support_from_generic_descriptor_count, relational_support_without_object_anchor_count, judge_calls_saved_by_contradiction_stop, contradiction_early_stop_count, supported_candidate_rejected_no_progress_count. |
| Level 5m — STRICT body-location semantics for real-artifact replay (fix: 5l located bodies from 148-char debug previews and reported passages_scanned / truncation / "validated" from them; the live-judge tier judged debug snippets and showed enabled=false despite the flag). Diagnostic only; zero live calls in default mode; no benchmark/accuracy/memory/generalization claim. (1) Provider cache discovery is RECURSIVE and never silently ignored: body discovery + `--inspect-schema` read the run_manifest `cache.path` pointer, EVERYTHING under `cache/` (rglob, any extension), and top-level `*cache*.json`; the shared cache report counts files/entries by provider, body/raw presence, `store_raw` headers, and `unrecognized_schema_count` (unparseable files are named, not skipped). (2) Body locator priority: (A) call-embedded body fields on the matched read call (`evidence_record.body/text/markdown/snippet/content`, min 600 chars to exclude bounded previews) → `call_embedded_body`; (B) provider recording cache via normalized prefix-URL match → `cache_stored_text`/`cache_raw_payload`; (C) debug snippet LAST-RESORT DIAGNOSTIC ONLY → `debug_snippet_only`, reported under `debug_snippet_scanned_count`, NEVER counted in `body_located_count`/`passages_scanned_count`, never live-judged, status `debug_snippet_scanned` with reason `read_body_not_persisted_legacy_run`/`debug_snippet_only_no_body`. (3) URL-matching INVARIANT: `matched_read=true` requires a URL relation (exact|prefix|host_only, host-exact; both directions since both URLs may be truncated); a candidate-id coincidence alone is NOT a match; `matched_read_url`/`matched_body_url` recorded separately; pinned 0 `matched_read_with_no_url_match_method_count`. (4) Truncation claims require an ACTUAL body: `body_truncated_before_relevant_passage(raw_unavailable)` only when a cached/stored body is at/near the adapter cap (or fetch_meta says truncated), raw is unavailable, and the scan found no anchors — never from a debug preview. (5) Pipeline statuses split: reconstructed → read_matched → body_located → passages_scanned (actual bodies) | debug_snippet_scanned (diagnostic) → judged → closed; overall_status is `validated` ONLY when something was judged/closed on an actual body, else `reconstructed_passages_scanned_rejudgment_pending` / `reconstructed_debug_only` / `reconstructed_body_missing`. (6) Live-judge tier judges ONLY actual-body passages (`live_rejudgment_source` recorded; recorded-rejudgment cache lookups are now PER OBLIGATION); with obligations but no actual bodies it reports `enabled=true` + `live_judge_skipped_reason=no_body_passages_to_judge` + `live_model_calls=0`. (7) Future-run persistence wired: `--read-cache-store-raw` (bounded raw bodies for read-class tools via `fetch_meta.raw_text` → cache `raw` field, cap `read_cache_raw_chars`=40000) and `--read-max-chars` (configurable page_fetch/firecrawl_scrape caps) in run_live; `read_judgment_max_chars`=20000 (judge input stays bounded by passage windows, so a larger retrieval body does not grow judge prompts); export keeps full untruncated `source_url` + the actual passage windows. Strict answer gate, contamination safety, replay, policy-memory hygiene UNCHANGED; demo data artifacts byte-identical. | `eval/replay_validation.py` (`_discover_cache_files`/`_extract_entry_bodies` + cache report, strict locator priority + `_MIN_REAL_BODY_CHARS`, `_match_read_call` URL invariant, per-obligation `_recorded_rejudgment`, strict `_summarize_legacy` + `_REAL_BODY_SOURCES` + partial overall statuses, `inspect_run_schema` recursive cache report); `scripts/validate_read_judge_replay.py` (body-source/url-match/cache summary lines, tier from loader); `scripts/run_live.py` (`--read-cache-store-raw`/`--read-max-chars`, RecordingCache store_raw, provider read caps); `live/providers.py` (read_max_chars/read_raw_chars wiring; wrapper moves `fetch_meta.raw_text` into the cache `raw` field); `tools/page_fetch.py` + `tools/firecrawl.py` (`raw_chars` bounded raw exposure); `agent/read_judgment.py` (`read_judgment_max_chars`=20000, `store_raw_read_tools`/`read_cache_raw_chars`); `tests/test_strict_body_location_5m.py` (11: cached body advances, call-embedded body recognised, debug snippet never counts as body/passages, matched_read always has url method, unrelated host not matched, truncation never from snippet, truncation for actual capped body, live skips on debug-only with reason, live records source for actual body, inspect-schema recursive provider cache, unrecognized schema counted). |
| Level 5l — FIX: reconstruct real 5g obligations from the STRUCTURED interpretations path (5k only scanned 5h-era frontier events, which a pre-5h 5g trace cannot contain → 0 obligations). Diagnostic only; zero live calls in default mode; no benchmark/accuracy/memory/generalization claim. (1) `_reconstruct_obligations` now reads `candidate_frontier.interpretations[].candidate_assertions[].judgments[]` — every `judgment=="requires_read"` yields an obligation with `reconstruction_method="structured_interpretations"`: candidate_id from the assertion's `canonical_candidate_ids[slot]`, slot from the judgment's `slot_id`/`proposed_slot_ids` (best-match to the constraint's `applies_to`), constraint from the judgment, source_url/role/domain from the parent interpretation; an `requires_read_constraint_ids` entry without a judgment detail is KEPT with `reconstruction_missing_fields=["judgment_detail"]`; nothing is dropped for missing ids (kept with `legacy_missing_candidate_slot_or_constraint`). Persistence caps are surfaced (`reconstruction_coverage_bounded` from `events_truncated`/interpretations[:20]). (2) Reads matched from `record["calls"]` (firecrawl_scrape/page_fetch + task_action read) → `matched_read`/`read_tool`/`read_success`/`read_chars`. (3) URL normalization: persisted URLs are truncated (`source_url[:160]`/`url[:300]`), so matching is host-exact + longest-path-prefix (`url_match_method=exact|prefix|host_only|none`; `source_url_mismatch` only when no host match). (4) Body location reports honestly: `body_source=cache_stored_text|cache_raw_payload|debug_snippet_only|not_found`, `store_raw_was_enabled` (from cache header), `raw_unavailable`; truncation asserted as `body_truncated_before_relevant_passage(raw_unavailable)` only when the raw payload is also unavailable (it states the cached portion lacks the passage, not the page). (5) Passage anchors add the assertion `candidate_text` (still never gold). (6) Live-judge tier state is now UNAMBIGUOUS: `live_judge_tier.enabled` reflects the flag, with `live_judge_skipped_reason` (`no_reconstructed_obligations`/`no_passages_to_judge`) — fixing the prior bug where the flag showed `enabled=false`. (7) `--inspect-schema` light zero-call probe (`inspect_run_schema`): per record top-level keys, judgment-verdict counts, events_truncated, calls-by-tool, cache files + whether entries carry raw. (8) Future-run persistence: `firecrawl_scrape` snippet cap is now configurable (`max_chars`, default 4000) + records `fetch_meta`; `export_read_judge_replay` keeps untruncated `source_url`. Strict answer gate, contamination safety, replay, policy-memory hygiene UNCHANGED; demo data artifacts byte-identical. | `eval/replay_validation.py` (`_reconstruct_obligations` structured-interpretations path + `_best_slot`/`_constraint_slots`, `_norm_url`/`_match_url`/`_match_read_call`, honest body reporting + `store_raw`/`raw_unavailable`, `inspect_run_schema`, unambiguous `live_judge_tier`, extended `ObligationOutcome`); `scripts/validate_read_judge_replay.py` (`--inspect-schema`, live-tier from loader); `tools/firecrawl.py` (configurable `FirecrawlScrape.max_chars` + `fetch_meta`); `fixtures/replay/legacy_run/` (rebuilt to the real structured schema: `interpretations[].candidate_assertions[].judgments[]` with a truncated 160-char source_url, a `store_raw` body whose fact sits past char 4000, and a missing-judgment-detail assertion); `tests/test_structured_reconstruction_5l.py` (10: structured reconstruction not events, missing-detail kept, truncated-url prefix match, norm/match-url helpers, raw-payload body + store_raw header, body-missing precise reason, live-tier enabled+skip-reason, live-tier disabled explicit, inspect-schema verdicts+raw flag, coverage-bounded surfaced). Offline: `python scripts/validate_read_judge_replay.py --artifacts <run> --json`; schema probe: `… --inspect-schema`; live-judge: `… --allow-live-judge --max-judge-calls 20`. |
| Level 5k — REAL-artifact replay loader + opt-in capped live-judge tier + future-run persistence + bounded re-read (extends 5j; zero live calls by default). Diagnostic only; no benchmark/accuracy/memory/generalization claim. (1/2) `eval/replay_validation.load_legacy_run` consumes a real `results/live` run dir (not only synthetic fixtures): it reads `debug_questions.jsonl`, the `cache/` RecordingCache(s), and `llm_evidence_judge_cache.json`, and **reconstructs** requires_read obligations from a pre-5h trace (`evidence_judgment_requires_read` events + candidate/slot/constraint + a later read of the same url), marked `reconstructed_from_legacy_trace=true`. Per obligation it reports how far the pipeline got — `reconstructed → body_located → passages_scanned → judged → closed` — with a PRECISE per-stage reason (`no_pending_read_judgment_events`, `read_event_found_but_body_missing`, `rejudgment_prompt_not_in_cache`, `body_truncated_before_relevant_passage`, `rejudgment_call_budget_exhausted`, …); it does NOT collapse to a generic `unvalidated_cache_miss` when inspectable artifacts exist. (3) Deterministic passage scan runs on the REAL cached body with anchors from question/constraint terms/target descriptor/recorded subject aliases (**never gold**); when the raw provider payload (`store_raw`) is fuller than the 4000-char stored body it scans the raw payload (`body_source=raw_cache_payload`) and records `cached_payload_chars` vs `stored_body_chars` + `passages_found_beyond_4000` — validating beyond-cap retrieval on real data with zero spend; `body_truncated_before_relevant_passage` is asserted only when the fuller payload is also exhausted. (4) Opt-in `--allow-live-judge --max-judge-calls N` tier: ONLY the new targeted re-judgment may call the model; all tool/provider data stays from cache (never fetches); hard-capped + **fail-closed** (`rejudgment_call_budget_exhausted`); each verdict recorded into the run judge cache so the next replay is fully offline; `live_model_calls` reported accurately; **off by default** and not exercised by any test (tests use a deterministic local stub). (5) `export_read_judge_replay` persists PendingReadJudgment records + read lifecycle events + per-url `fetch_meta` + bounded passage windows + closure codes (contamination-safe, no gold) for exact future replay without reconstruction. (6) `ReadJudgmentConfig` adds a read-cap policy (config, not hardcode): global `page_fetch` cap stays 4000; `read_judgment_max_chars`=12000 for read-judgment-backed reads (judge input already bounded by passage windows); a single bounded `reread_max_chars`=16000 re-read via `plan_reread_for_truncation` (LIVE runs only; replay never fetches) fires at most once when a truncated read cannot close a pending obligation (`read_reread_due_to_truncation`). Strict answer gate, contamination safety, replay, policy-memory hygiene UNCHANGED; demo data artifacts byte-identical. | `eval/replay_validation.py` (`load_legacy_run`, `_reconstruct_obligations`, `_load_provider_bodies`, `_anchor_terms_for_constraint`, `_live_rejudge`, `export_read_judge_replay`, `STAGE_REASONS`, extended `ObligationOutcome`); `scripts/validate_read_judge_replay.py` (`--allow-live-judge`/`--max-judge-calls`); `agent/read_judgment.py` (read-cap policy knobs); `agent/candidate_frontier.py` (`plan_reread_for_truncation`); `activegraph_pack/events.py` (`read_reread_due_to_truncation`); `fixtures/replay/legacy_run/` (synthetic legacy-run dir: `debug_questions.jsonl` + `cache/provider_cache.json` with `store_raw` body past char 4000); `tests/test_legacy_replay_5k.py` (11: reconstruct+scan offline, raw-payload beyond 4000, no-requires-read precise reason, absent→unvalidated, live-tier off-by-default zero calls, live closes+records+offline-replay, fail-closed at cap 0, bounded persistence replayable, read-cap config policy, re-read only when truncated+unresolved+pending+once, 5j fixture still green). Offline command: `python scripts/validate_read_judge_replay.py --artifacts <run>`; live-judge: `… --allow-live-judge --max-judge-calls 20`. Metrics: reconstructed/body_located/passages_scanned/judged/closed_count, passages_found_beyond_4000_count, cached_payload_chars vs stored_body_chars, live_model_calls, stage_reason_counts. |
| Level 5j — OFFLINE validation pass (no live calls): prove the 5h/5i mechanisms close or explicitly explain the 5g failure surfaces, using replayable fixtures. The referenced live 5g artifacts (`results/live/browsecomp-llm-evidence-judge-5g-smoke-001`) are **absent** in a fresh container (gitignored live outputs), so the artifacts replay honestly reports `unvalidated_cache_miss` — never a silent pass — and the mechanism is proven on a committed synthetic, diagnostic-only fixture (generic placeholder names; no gold/domains/URLs from any benchmark). (A) `eval/replay_validation.py` replays a trajectory through current code: a `requires_read` becomes a `PendingReadJudgment`; a later read of the same `source_url` routes the cached BODY into a targeted re-judgment of the same triple; the obligation closes with `resolved_full_support`/`…partial`/`…contradiction`/`…irrelevant`/`closed_no_relevant_passage`/`closed_read_unavailable`, stays `requires_read_still_open`, or is `unvalidated_cache_miss`. Replay metrics: `replay_pending_read_judgment_count`, `replay_read_judged_after_read_count`, `replay_pending_read_closed_count`, `replay_requires_read_still_open_count`, `replay_pending_read_unvalidated_cache_miss_count`. (B) **Truncation finding: the 4000-char cap is an ADAPTER cap** — `tools/page_fetch.PageFetch.max_chars` (default 4000), applied to parsed page text BEFORE it is stored/judged; `page_fetch` now records `fetch_meta` (`fetched_chars`/`stored_body_chars`/`adapter_max_chars`/`body_truncated_for_storage`/`truncation_origin`); the fixture proves `extract_passages` finds the resolving fact at char offset ~6222 (>4000) when the body/cap allow it. Pinned 0: `judge_reused_truncated_excerpt_after_full_read_count`, `read_head_only_judgment_count`. (C) Fixture effects: judge calls 10→1 (`fixture_judge_call_reduction_ratio`=0.9, ≥50% target), `fixture_valid_candidate_verdict_regression_count`=0, generic/title/chrome promotions=0, `explicit_location_mismatch_promoted_count`=0. (D) `eval/metric_derivation.py` recomputes event-backed metrics from the event log and asserts parity with inline counters (`verify_metric_derivation`); inline-only counters are documented with reasons. (E) DIAGNOSTICS ONLY: `parser_referent_diagnostics` estimates duplicate-referent slots + blocks target↔subject; the parser-prompt nudge is STAGED (not applied — would change the parser prompt hash/cache) and NO coreference cost reduction is claimed. (F) Read terminology reconciled: `read_interpreted` carries distinct `read_added_candidates`/`read_added_constraint_support`/`read_resolved_pending_judgment` (a read that only adds candidates no longer looks like it resolved support). Strict answer gate, contamination safety, replay, policy-memory hygiene UNCHANGED; demo data artifacts byte-identical; NO benchmark/accuracy/memory/generalization claim added. | `eval/replay_validation.py` (`run_replay`, `run_triage_promotion_fixture`, `validate_artifacts_dir`, `deterministic_replay_judge`, closure codes); `eval/metric_derivation.py` (`derive_metrics_from_events`, `verify_metric_derivation`, `INLINE_ONLY`); `scripts/validate_read_judge_replay.py`; `fixtures/replay/read_judge_loop_fixture.json` + `triage_promotion_fixture.json`; `tools/page_fetch.py` + `tools/base.py` (`fetch_meta` truncation accounting); `agent/read_judgment.py` (`PassageScan.input_chars`/`scanned_chars`/`first_hit_offset`/`passage_anchor_hits`/`passage_count`); `agent/candidate_frontier.py` (5j-F precise read-outcome fields + counters); `agent/coreference.py` (`parser_referent_diagnostics`); `tests/test_replay_validation_5j.py` (10: replay closes via full body, cache-miss-not-silent-pass, absent-artifacts→unvalidated_cache_miss, passages beyond char 4000, page_fetch cap field, obligation used full body, triage fixture ≥50% reduction + no unsafe promotion, event-derived metric parity, read-outcome fields distinct, parser referent diagnostics). Offline command: `python scripts/validate_read_judge_replay.py`. Metrics: replay_pending_read_*_count, fixture_judge_call_reduction_ratio, fetched_chars/stored_body_chars, read_resolved_pending_judgment_count, parser_duplicate_referent_slot_count_estimate. |
| Level 5i — source-subject extraction (I) + pre-judge triage / explicit-location filter / batch judging (K) + stable debug slice (N) + coreference SAFETY scaffolding (J). Preserves all 5h behavior; strict answer gate untouched; generic, deterministic-by-default, replayable, answer-free. (I) Every result/read yields a `SourceSubject` (subject_name/type/role, is_predicate_grounded, is_chrome_or_source_title_only, aliases, quote); candidate promotion uses it — a source title-only/chrome/generic-topic subject is not a candidate unless body predicate text grounds it; profile/official/database pages are structurally about their title subject; a `Title (YYYY)` work page is `title_or_work` (a film "The Founder" never binds a person slot). Pinned 0: `candidate_promoted_from_source_title_only_count`, `candidate_promoted_from_chrome_count`. (K1) Chrome/UI/definition/title-only candidates rejected BEFORE any judge call; `judge_calls_saved_by_prejudge_triage` counts savings; pinned 0 `judge_invoked_on_obvious_chrome_count` (+ source-title-only/definition for concrete tasks). (K2) Gazetteer-bounded (US states+DC), preposition-gated explicit-location filter: an org/place candidate whose source names a different location with no branch evidence is rejected pre-judge; ambiguous → `needs_location_support` kept; compatible → allowed; persons not filtered. Pinned 0 `explicit_location_mismatch_promoted_count`. (K3) Optional `EvidenceJudge.judge_batch` — one cached call per (source, candidate) over a bounded constraint set, per-constraint verdicts, post-model hard rules applied per constraint, blocking-contradiction early-stop, fallback to per-constraint; support materializes identically. (J) `propose_coreference` SAFETY scaffolding: proposes obvious same-referent person merges but NEVER auto-merges by default; blocks distinct-role / distinct-anchor (hotel/museum/restaurant/founder/birth-year) / target↔subject merges. Pinned 0 `invalid_coreference_collapse_count`; no judge-call reduction claimed. (N) `scripts/make_debug_slice.py` + `eval/debug_slice.py`: deterministic, dataset-immutable 10–20 item slice manifest with pinned `item_ids`, `headline_eligible=false`; `compare_runs` surfaces mechanism-detector deltas (debug labels only); `generate_claims` refuses headline/memory claims for a debug-slice report. Strict answer gate, contamination safety, replay, policy-memory hygiene UNCHANGED; accuracy NOT the criterion. | `agent/source_subject.py` (`SourceSubject`, `extract_source_subject`, `subject_supports_candidate`, `is_promotable_subject`); `agent/evidence_interpreter.py` (source-subject extraction in `interpret`, pre-judge triage gate `_note_prejudge_reject`, explicit-location `_gazetteer_match`/`_locations_in_context`/`_location_compat`/`explicit_location_terms`, batch-judge branch, 5i stats); `agent/evidence_judge.py` (`judge_batch` + `_batch_prompt` + batch counters); `agent/coreference.py` (`CoreferenceProposal`, `propose_coreference`, `coreference_metrics`); `eval/debug_slice.py` (`select_debug_slice`, `DebugSliceManifest`, `detector_deltas`, `is_debug_slice_report`); `scripts/make_debug_slice.py`; `scripts/compare_runs.py` (detector-delta surface); `scripts/generate_claims.py` (refuse debug-slice headline); `activegraph_pack/events.py` (source-subject + explicit-location events); `eval/metrics.py` (5i interpreter/judge aggregation, pinned-0 invariants); `tests/test_source_subject_triage_5i.py` (21: grounded person subject, title-only/definition rejected, definition headword not promoted, work page role-incompat, org subject promotable, relation needs object anchor, chrome not judged, triage saves calls, location mismatch rejected, ambiguity kept+flagged, compatible location allowed, batch matches per-constraint, batch preserves contamination rule, blocking contradiction early-stops, batch per-constraint replayable events, debug slice deterministic+ids, claims refused for slice, coreference proposes-not-merges, blocks distinct entities, blocks subject↔target, 5h invariants green). Metrics: source_subject_extracted/promoted/rejected_count, candidate_promoted_from_source_title_only_count (=0), candidate_promoted_from_chrome_count (=0), prejudge_rejected_count, judge_calls_saved_by_prejudge_triage, judge_invoked_on_obvious_chrome_count (=0), explicit_location_mismatch_rejected/ambiguous_kept/supported_count, explicit_location_mismatch_promoted_count (=0), batch_judge_calls_count, per_constraint_judge_calls_avoided_count, batch_judge_post_rule_downgrade_count, coreference_proposals/proposed_only/rejected_count, invalid_coreference_collapse_count (=0), target_subject_merge_blocked_count. |
| Level 5h — close the read→judge loop + target binding + anti-pollution (after a 5g smoke on two BrowseComp questions, diagnostic only; no benchmark allowlists/gold/domain rules/names). 5g made reads execute; 5h closes the loop. (A/B) **Read→judge loop closure + targeted passage retrieval**: a judge `requires_read` creates a persistent `PendingReadJudgment` on the exact `(candidate, slot, constraint, source_url)` triple (`read_required_by_judge`); when that source is read, its page BODY is routed back into a re-judgment of the SAME triple via `extract_passages()` (deterministic windows around constraint/target/alias/relation/year anchors, lexical-overlap fallback, bounded by `read_max_chars_total`/`read_passage_window_chars`/`read_max_passages_per_pending_judgment`), so the judge sees PASSAGES — never the truncated snippet, never only the head — and each obligation closes (full/partial/contradiction) or stays open with an explicit reason (still_unresolved/irrelevant/no_relevant_passage). Pinned 0: `judge_reused_truncated_excerpt_after_full_read_count`, `read_head_only_judgment_count`. (C/D) **Target-answer binding + priority**: first-class `bind_target_answer_slot` fires when a subject is supported but an answer-shaped target slot (year/date/number/name/title/venue/duration) is unbound; it pivots from subject aliases + target answer-shape anchors, OUTRANKS re-verifying a supported subject (demoted), a confirmed slot stops generating new candidates, and word-order query variants are deduped (`fuzzy_duplicate_no_progress_query`). Pinned 0: `target_answer_slot_filled_with_subject_count`, `blocking_target_slot_starved_count`. (E/F/G) **Anchor gate + seed floor + abstain admissibility**: proposal anchor coverage by NORMALIZED content-token overlap (multiword terms split; years→`<year>` class) so compact reformulations ("hotel 1955" for "hotel originally opened in 1955") pass; all-rejected-on-soft-anchor + anchor-rich → RELAXED gate instead of a generic seed (`generic_fallback_after_all_proposals_rejected_count` pinned 0); a hard task never executes a generic single-token seed (`generic_single_token_seed_executed_count` pinned 0); abstain inadmissible while budget+blocking+executable anchor-rich action exist (`abstain_with_budget_remaining_count` pinned 0). (H/L) **Source hygiene + staging**: a concrete entity task never READS a generic definition page (`generic_definition_source_read_count` pinned 0); dependent slots stage behind dependencies (`premature_founder_search_before_place_supported_count`/`premature_birth_year_search_before_founder_supported_count` pinned 0) while a confirmed candidate still expands to dependents. (M/O) **Detectors + labels**: `read_loop_open_count`/`read_success_no_evidence_added_count`/`proposal_gate_starvation_count` added as DEBUG labels only; candidate-local confirm reason renamed `slot_candidate_supported` (`debug_confirmed_label_when_answer_gate_false_count` pinned 0). Strict answer gate, contamination safety, replay, policy-memory hygiene UNCHANGED; accuracy NOT the criterion. Deferred (documented, not claimed): source-subject extraction (I), coreference collapse (J — safety invariant "never merge distinct entities" holds), batch judging + explicit-location hard filter (K), dev-slice runner (N). | `agent/read_judgment.py` (`PendingReadJudgment`, `extract_passages`, `build_anchor_terms`, `ReadJudgmentConfig`); `agent/candidate_frontier.py` (`_register_pending_read_judgment`/`route_read_into_pending_judgments`/`_judge_passage`, `bind_target_answer_slot` generation + `_build_bind_target_query` + target priority/demotion, `_is_generic_single_token` seed floor, `abstain_admissible`, `_is_concrete_entity_task`/`_is_generic_definition_source`, `_dependency_unsupported` staging, `_metrics_5h`); `agent/llm_frontier.py` (`_anchor_terms` token-split + `_query_anchor_tokens`, `_is_fuzzy_duplicate`, `_proposal_is_anchor_rich` + relaxed-gate recovery, anchor-gate diagnostics); `agent/search_loop.py` (`read_candidate_id` wired into frontier ingest); `activegraph_pack/events.py` (read→judge loop + bind + hygiene + relaxed-gate events); `eval/metrics.py` (5h aggregation + pinned-0 invariants); `scripts/debug_run_failures.py` (READ→JUDGE LOOP + TARGET BIND blocks); `tests/test_read_judge_target_5h.py` (19: pending→routed re-judgment, resolves/keeps-open with reason, passages-not-head, descriptor passage hits, bind on supported-subject+unbound-target, date slot rejects person, bind outranks verify, fuzzy dedupe, compact reformulation passes anchor gate, relaxed-gate-not-generic-seed, generic seed blocked, abstain inadmissible, definition page not read, chrome not judged, distinct slots not collapsed, dependent slot deferred, detectors+labels present, read-loop-open flagged, passages deterministic). Metrics: requires_read_count, requires_read_resolved_by_read_count, requires_read_unresolved_after_successful_read_count, read_passage_hits/judged_count, bind_target_answer_slot_actions/selected_count, target_answer_slot_filled_with_subject_count (=0), generic_single_token_seed_executed_count (=0), generic_definition_source_read_count (=0), generic_fallback_after_all_proposals_rejected_count (=0), premature_founder/birth_year_*_count (=0), read_loop_open_count, debug_confirmed_label_when_answer_gate_false_count (=0). |
| Level 5g hardening — reads that execute + prompt-text-is-not-evidence (after a 5f smoke on two BrowseComp questions, used only as a diagnostic; no benchmark allowlists/gold/domain rules/names): make the loop internally consistent. (1) **Read-intent lifecycle is explicit and ActiveGraph-native** — a desired read emits `read_desired`; with no reading tool → `read_blocked_disallowed_tool` (unexecutable `read_blocked_no_tool`); with a tool, the candidate's own clean non-contaminated `source_urls` drive the read via `_read_candidate_url` (so a read executes even when the current observations carry no matching snippet) → `read_selected` + a `read_candidate_source` StepPlan; a read whose candidate has only a query string is recorded `read_blocked_no_url` (unexecutable) and is NEVER silently translated into a search (`selected_read_action_translated_to_search_count` pinned 0; `read_desired_count`/`read_selected_count`/`read_blocked_no_url_count` surfaced). Clean URL = `http(s)://`, no spaces, len>12 (`_is_clean_url`); contaminated source URLs are never stored for reading. (4) **Prompt text is not evidence** — `HypothesisTable.ingest_evidence` resolves a *blocking* constraint only from a non-contaminated source carrying a **distinctive anchor**: a year (`\b(1[5-9]\d{2}\|20\d{2})\b`) OR a proper-cased term drawn from the constraint's own `text_span`; a clean snippet that merely echoes generic question terms ("driving distance in miles") never resolves a blocker, and a freshly-built frame has no evidence-free resolved blockers (`initial_blocking_constraint_resolved_without_evidence_count` pinned 0). (3) **Support consistency** — a `full_support` judgment that never materializes into the slate's supported constraints is pinned 0 (`full_support_judgment_without_materialized_constraint_support_count`). (7) **Judge pre-gate** — chrome/UI/nav + source-title-without-predicate candidates are rejected before the judge runs (`judge_invoked_on_ui_or_navigation_count`/`judge_invoked_on_source_title_without_predicate_count` pinned 0; `judge_calls_saved_by_prefilter`). Strict answer gate, contamination filters, replay, policy-memory hygiene UNCHANGED; accuracy is NOT the criterion. Deferred (documented, not weakened): generic `bind_target_answer_slot` action (req 6), parser-fallback variable/constant classifier (req 5), exa/firecrawl alternate-URL read fallback (req 2), read-intent states as first-class graph objects (req 9). | `agent/candidate_frontier.py` (`_is_clean_url`, `SlotCandidate.source_urls`, `_assign` stores clean non-contaminated url, `_read_candidate_url`, read branch emits `read_desired`/`read_selected`/`read_blocked_no_url`/`read_blocked_disallowed_tool` + unexecutable reasons, read-intent counters + invariants in `metrics()`); `agent/hypothesis_table.py` (`_YEAR_RE`, distinctive-anchor gate on blocking-constraint resolution, contamination guard); `activegraph_pack/events.py` (READ_DESIRED/READ_SELECTED/READ_BLOCKED_NO_URL/READ_BLOCKED_DISALLOWED_TOOL/READ_BLOCKED_UNSAFE_OR_CONTAMINATED_URL); `agent/search_loop.py` (`initial_blocking_constraint_resolved_without_evidence_count` in frame_coverage); `eval/harness.py`/`eval/metrics.py` (read-intent + support-consistency + pre-judge aggregation, pinned-0 invariants); `scripts/debug_run_failures.py` (READ INTENT block); `tests/test_read_and_support_5g.py` (10: read executes via candidate URL not search, read-without-URL→blocked not search, contaminated URL not stored, `_is_clean_url`, blocker not resolved from contaminated/generic-overlap, blocker resolves with distinctive anchor, initial-resolved-without-evidence=0, judge not invoked on chrome, read-intent metrics present). Metrics: read_desired_count, read_selected_count, read_blocked_no_url_count, read_blocked_disallowed_tool_count, read_not_executed_after_requires_read_count, forced_read_to_executed_ratio, selected_read_action_translated_to_search_count (=0), initial_blocking_constraint_resolved_without_evidence_count (=0), full_support_judgment_without_materialized_constraint_support_count (=0), judge_invoked_on_ui_or_navigation_count (=0), judge_invoked_on_source_title_without_predicate_count (=0), judge_calls_saved_by_prefilter. |
| LLM evidence judge (Level 5f): a narrow, cached, replayable LLM that decides whether a source excerpt `full_support`/`partial_support`/`contradiction`/`irrelevant`/`requires_read` for a specific candidate/slot/constraint triple. NOT a planner or answerer — never produces a final answer, never sees gold, never writes policy memory, never weakens the answer gate. Inputs: slot id/role/descriptor, candidate text/id/aliases, constraint text/testable_claim/evidence_needed/how_to_test, source title/url/domain/role + contamination flags, snippet/excerpt, deterministic interpretation. Output schema: judgment, candidate_role_fit, source_role_fit, supported/unsupported/contradicted facets, quote, rationale, confidence, requires_read_reason, candidate_aliases, safety_notes. Hard rules enforced AFTER the model (a stub that over-credits can't break them): contaminated/noise source → never full/partial support; full_support REQUIRES a quote tying candidate to predicate; title overlap alone → partial; chrome never reaches the judge. Flow: full_support→`constraints_supported` (visible to gate); contradiction→candidate rejected; partial_support→`constraints_partial` (raises EIG, NEVER resolves a blocker); requires_read→read of that source outranks more snippet search (or `skipped_read_after_requires_read`); judge aliases enter the canonical alias registry so verifier proposals resolve. Cached by `prompt_fingerprint\|model\|triple_hash`; dry-run/replay = zero model calls, deterministic recognizer fallback. Default OFF; `--enable-llm-evidence-judge`/`--llm-evidence-judge-model` (requires `--enable-task-frame`); easy questions skip it. | `agent/evidence_judge.py` (`EvidenceJudge`, `EvidenceJudgment`, `enforce_hard_rules`, `_det_to_judgment`); `agent/evidence_interpreter.py` (per-(cand,slot,con) judge call in `_assert_candidate`; assertion `slot_partial`/`slot_requires_read`/`candidate_aliases`/`judgments`); `agent/candidate_frontier.py` (`SlotCandidate.constraints_partial`/`requires_read_constraint_ids`/`aliases`; attach applies partial/requires_read/aliases + emits judgment events; read boost for `evidence_judge_requires_read`; partial→evidence_score not confirmable); `agent/prompts.py` (`evidence_judge` v1); `agent/planner.py`/`search_loop.py` (per-attempt judge wiring + `enable_llm_evidence_judge`); `activegraph_pack` (1 object + 5 relations + 9 events); `eval/projection.py` (`evidence_judgment` nodes + relations); `eval/metrics.py` (judge aggregates + invariants); `scripts/_common.py`/`run_live.py`/`config/default.yaml`/`live/providers.py` (`build_evidence_judge_model_fn`); `tests/test_evidence_judge.py` (15: full→materialize+state, partial not blocking, contradiction rejects, contaminated can't support, full-without-quote→partial, requires_read schedules read, requires_read skipped no source, chrome irrelevant+not judged, alias resolves verifier, replay zero calls, one read→multiple supports, partial-only not answerable, support-consistency no drop, easy skips judge, answer-free memory). Metrics: llm_evidence_judge_calls/cache_hits/replay_hits, llm_full/partial/contradiction/requires_read/irrelevant_count, full/partial_support_from_contaminated_source_count (=0), support_from_llm_judge_rate, llm_requires_read_scheduled_rate. |
| Research loop: reads close evidence + discriminative/support-consistent scheduling (Level 5e): live traces showed candidates binding to slots but constraints rarely closing (`ev→slot` true, `ev→cons` false), `read_candidate_source` starved by EIG even when only a page body can support a constraint, and hard problems stuck at stage_depth=1. Generic fixes (no benchmark/domain rules): (1) **read scheduling** — `read_candidate_source` boosted when a candidate has a source + unresolved constraints but snippets gave no support (`read_to_convert_candidate`), and **forced** after N (default 2, configurable `force_read_after_n`) consecutive search/verify with no full/partial support (`forced_read_after_no_support`); search/verify EIG decays with the no-support streak so reads/pivots take over; read value higher for high-evidentiary source *roles* (profile/official/scholarly/database/article), not domains; one read closes multiple constraints (interpreter emits multiple candidate-constraint assertions from one body). (2) **discriminative-first** — `propose_step_action` records `chosen_constraint_specificity` + `discriminative_reason` computed generically (term rarity, numeric/date + named-entity anchors, #slots constrained). (3) **registry alias resolution + nonexistent breakdown** — `resolve_candidate` classifies a miss (`exists_in_other_slot`/`exists_as_weak_observation`/`exists_but_rejected`/`exists_but_stale`/`normalized_alias_found`/`truly_nonexistent`); weak observations indexed so an extracted mention is never "truly nonexistent". (4) **support-consistency invariant** — if per-candidate support didn't land on the evidence record, a `support_dropped` event records where (never silent). Answer-support gate UNCHANGED + strict. Deferred (documented, not weakened): full LLM evidence judge, learned provider routing, explicit Answer Support Contract. | `agent/candidate_frontier.py` (`generate_frontier_actions` read boost/forced-read + streak decay, `_slot_no_support_streak`/`force_read_after_n`, `_discriminativeness`, `resolve_candidate` breakdown + `weak_observation_index`, `_assign` `source_role`/`read_depth` + support-from-read, `support_dropped` invariant, `read_scheduled`/`read_interpreted` events, `_HIGH_VALUE_READ_ROLES`, `SlotCandidate.source_role`/`read_done`, `StepPlan.discriminative_reason`/`chosen_constraint_specificity`); `agent/llm_frontier.py` (lookup `breakdown` in events); `activegraph_pack/events.py` (`support_dropped`/`read_scheduled`/`read_interpreted`); `eval/metrics.py` (`_frontier_metrics` read/support aggregates + `_llm_frontier_metrics` `nonexistent_candidate_breakdown`/`canonical_candidate_alias_resolution_rate`); `scripts/debug_run_failures.py` (READ SCHEDULING block); `tests/test_research_loop.py` (8: forced read after no-support, one read→multiple constraints, support-consistency no silent drop, nonexistent breakdown, other-slot resolution, discriminative reason recorded, streak resets on support, verifier alias resolution). Metrics: read_starvation_count, forced_read_after_no_support_count, read_executed_count, support_from_read_count/rate, read_after_search_rate, support_asserted_not_consumed_count, nonexistent_candidate_breakdown, canonical_candidate_alias_resolution_rate. |
| Evidence interpretation unification (Level 5d.1): the inspectable interpreter trace showed three generic failures — (1) evidence reached slots but not constraints (`ev→slot` true, `ev→cons` false, `supports=[]`), (2) the verifier rejected interpreter-extracted candidates as `nonexistent_candidate:Cristina Ortiz` (assertions and the canonical SlotCandidate registry were separate object systems), (3) `role=unknown` candidates fanned out into every slate and chrome/date fragments entered slates. Fix (no new architecture, no BrowseComp rules): (1) an accepted CandidateAssertion now **materializes/updates the canonical SlotCandidate** (`attach_constraint_support_from_interpretation`), records `assertion_id→candidate_id` + a per-slot candidate-text index; the verifier resolves a proposal candidate by **text or id** (`resolve_candidate`, exact+normalized variants, per-slot then cross-slot) so extracted candidates are found (rich `candidate_lookup_failed` debug otherwise). (2) Constraint support is **attached from generic recognizers** (`recognize_constraint_support`: ≥2-anchor overlap, temporal year+predicate pattern, OR one distinctive anchor from a corroborating source — professional_profile/official/scholarly/database/directory — on a role-compatible candidate), requiring an acceptable non-contaminated source; candidate `constraints_supported` is set from these decisions, never recomputed from arbitrary overlap. (3) `unknown`-role entities become recorded `weak_observation_not_candidate` (no fan-out); months/weekdays→`date_or_time` (date slots only), venue-suffix names→organization, leading chrome (`Browse…`/`Login…`) + generic-type-only phrases (`TV Shows`) rejected; `_slot_compatible` drops the unknown wildcard. (4) Repair triggers broadened to noise-term-in-query + prompt-language/crossword queries (no distinctive anchor). | `agent/evidence_interpreter.py` (`recognize_constraint_support`/`_distinctive_anchors`, `_normalize_role`/`_hygiene_reason`/`_slot_compatible`, `_ORG_SUFFIX`/`_MONTHS`/`_WEEKDAYS`/`_GENERIC_TYPE_WORDS`/`CORROBORATING_ROLES`, `CandidateAssertion.assertion_id`/`canonical_candidate_ids`/`slot_support`); `agent/candidate_frontier.py` (`resolve_candidate`, `attach_constraint_support_from_interpretation`, `candidate_text_index`/`assertion_to_candidate`, `_assign` applies attached support + returns id; leading-chrome `_noise_kind`); `agent/llm_frontier.py` (`validate_proposal` text→id resolution, `repair_trigger_reason` noise/prompt-language triggers, `_query_lacks_distinctive_anchor`); `activegraph_pack` (9 events + 5 relations); `eval/projection.py`/`metrics.py`/`harness.py`/`debug_run_failures.py`; `tests/test_evidence_unify.py` (16: materialize canonical, verifier resolves by text, extracted-then-verified not nonexistent, Cristina+corroborated-single-anchor support, hotel-year supports hotel not person slot, org local-predicate, unknown no fan-out, TV Shows/weekday rejected, ev→slot-true/cons-false explained, repair on noise/prompt-language, answer-free memory). Metrics: evidence_to_slot/constraint_link_rate, ev_slot_true_cons_false_count, accepted_candidate_without_constraint_support_count, canonical_candidate_resolution_rate, verifier_nonexistent_but_extracted_candidate_count, extracted_candidate_not_in_registry_count, deterministic_query_marked_ok_but_repaired_count, deterministic_query_bad_but_not_repaired_count. |
| Evidence interpretation layer (Level 5d): retrieval is not understanding — even with good frames/queries, slates filled with page chrome (`Datasets`/`Hugging Face`/`Translate`/`Login`/`Merriam`/`Username Generator`/`FOUNDER Definition`) and `constraint_sup` stayed ~0 from arbitrary term overlap; a correct `Cristina Ortiz` result was never promoted. Fix: a generic `EvidenceInterpreter` turns each result into structured assertions BEFORE it can touch a slate. Per result it emits an `EvidenceInterpretation`: a **source role** (`primary_source`/`professional_profile`/`official_page`/`article`/`scholarly_paper`/`database_record`/`directory_listing`/`social_page`/`forum_page`/`generic_definition_page`/`benchmark_contaminated`/`ui_or_navigation_noise`/`unknown`) classified by generic source-TYPE + page-INTENT (NOT a BrowseComp domain stoplist; platform hosts only TYPE a role, e.g. `linkedin.com/in`→professional_profile); **candidate assertions** (accepted, or rejected-with-reason: `generic_definition_noise`/`ui_navigation_noise`/`benchmark_contaminated_source`/`source_platform_noise`/`role_incompatible`/`no_slot_compatible_evidence`/`unsupported_by_selected_constraint`) with inferred role, proposed slot(s), supports/contradicts constraints, evidence quote, confidence; **constraint assertions** (supports/contradicts/irrelevant/insufficient + quote). Slates are populated ONLY from accepted assertions; constraint support changes ONLY via a constraint assertion from a non-noise, role-compatible source — never raw n-grams/overlap. Deterministic + ALWAYS ON; optional `--enable-llm-evidence-interpreter`/`--llm-evidence-interpreter-model` (requires `--enable-task-frame`) adds a cached/replayable LLM source-role re-classification keyed by `prompt_hash\|evidence_hash\|frame_hash\|model` (replay/dry-run = zero model calls). Answer-free; nothing reaches policy memory. | `agent/evidence_interpreter.py` (`EvidenceInterpreter`, `classify_source_role`, `CandidateAssertion`/`ConstraintAssertion`/`EvidenceInterpretation`, `SOURCE_ROLES`/`HARD_NOISE_ROLES`); `agent/candidate_frontier.py` (ingest drives `_assign` from accepted assertions; expanded `_noise_kind` sets/phrases; `interpretations`/`to_debug`); `agent/planner.py`/`search_loop.py` (per-attempt interpreter wiring + `enable_llm_evidence_interpreter`); `activegraph_pack` (5 objects + 9 relations + `EVIDENCE_INTERPRETATION_EVENTS`); `eval/projection.py` (`_project_evidence_interpretation`); `eval/metrics.py` (`_evidence_interpretation_metrics`) + `harness.py` (`_interpretation_stats`); `eval/debug.py`/`scripts/debug_run_failures.py`; `scripts/_common.py`/`run_live.py`/`config/default.yaml`/`live/providers.py` (`build_evidence_model_fn`); `tests/test_evidence_interpreter.py` (11: source-role classification, dictionary→no founder candidate, UI/nav rejected, contaminated cannot support, professional profile→candidate, Cristina Ortiz promoted, restaurant→place not founder, hotel-1955 supports only tied to hotel slot, no support from noise overlap, replay projection has evidence_interpretation, answer-free memory). Metrics: evidence_interpretation_count, source_role_counts, candidate/accepted/rejected_assertion_count, candidate_assertion_rejection_counts, constraint_assertion_support/contradiction_count, constraint_support_from_interpretation_rate, noise_candidate_rejection_rate, source_role_noise_rate, candidate_promotion_from_evidence_count, has_gold_but_not_promoted_count (debug-only). |
| Frontier CONTROLLER (Level 5b): the candidate-slate scheduler is promoted from shadow/projection to an OPTIONAL controller that drives tool selection. `--enable-frontier-controller` (requires `--enable-task-frame`; hard config error otherwise). Default = shadow: the old planner drives, but the frontier records `frontier_recommended_action`/`planner_actual_action`/`action_agreement` per step. Active: `propose_step_action` selects by EIG and translates to a search query built from the slot's most **discriminative** blocking constraint (never a bare repeat of the target descriptor — TV frame opens on "actor born Tennessee", not `"90s TV series" 90s TV series`), a candidate+slot+constraint read, or a terminal answer/abstain. Every executed call links a `frontier_action_id` (`tool_call_from_frontier_action` edge); an unexecutable action records `frontier_action_unexecutable` and falls back to the old planner for that step. Answer-support gate UNCHANGED; controller decisions are event/trace-backed and replay-reconstructable. | `agent/candidate_frontier.py` (`StepPlan`, `propose_step_action`, `_build_query` discriminative-first, `record_execution`/`record_unexecutable`); `agent/search_loop.py` (controller branch + shadow recording + `CallRecord.frontier_action_id`); `scripts/_common.py`+`scripts/run_live.py`+`config/default.yaml`+`live/settings.py`/`runner.py`/`eval/manifest.py`/`report.py` (`--enable-frontier-controller` flag + guardrail); `eval/projection.py` (`tool_call_from_frontier_action`/`hypothesis_updated_after_frontier_action`, executed status); `eval/metrics.py`/`harness.py`/`debug_run_failures.py`; `tests/test_frontier_controller.py` (11: discriminative-first, no generic repeated query, read triple, shadow records recommendation, active drives + links calls, unexecutable→fallback, easy skip, replay determinism, offline rescore w/o providers, controller-requires-task-frame guardrail, answer-free memory). Metrics: frontier_controller_used_rate, frontier_planner_agreement_rate, frontier_action_execution_success_rate, frontier_fallback_to_old_planner_count, frontier_expected_gain_realized, tool_calls_from_frontier_actions, first_action_discriminative_constraint_rate, generic_first_query_rate. |

## Commands that pass with no keys/network

```
python -m pytest -q                              # 68 passed
python scripts/run_synthetic_full.py --run-id demo
python -m pytest -q tests/test_real_data_shape.py
python scripts/run_ablations.py
python scripts/validate_live_readiness.py        # 0 failures (warnings only)
python scripts/preflight_first_live_run.py       # 0 blockers; writes dry-run manifest
python scripts/estimate_live_cost.py --optimize 10 --confirm 20
python scripts/inspect_memory_snapshot.py results/demo/memory_snapshot.json   # leakage PASS
python scripts/hash_artifacts.py results/demo
python scripts/generate_claims.py results/demo/report.json   # REFUSES perf claims
python scripts/docs_check.py                      # required docs present, no overclaim
python scripts/fork_offline_ablation.py results/live/<run_id> --out <id>-fork  # offline; replay-only, refuses to spend
python scripts/run_live.py --dataset real-shaped --optimize 5 --confirm 10 \
    --budgets 1 --conditions closed_book,no_memory_search   # DRY-RUN, no calls
make live-dry-run                                 # no provider calls
make check
```

**Live executor: wired, not executed.** `scripts/run_live.py` calls providers
ONLY with `--execute` (never run here). First provider-calling run is pending;
see `docs/LIVE_LADDER.md`.

## ◑ Partially verified claims

| claim | status |
| --- | --- |
| BrowseComp / LiveBrowseComp adapters load real data. | Decode/round-trip helpers tested (`datasets/browsecomp.py`); real-data load is implemented but not exercised in CI (no data committed). Missing data raises a clear `DatasetUnavailable`. |
| Live search/answer adapters work end-to-end. | Adapters are implemented and env-gated (`tools/*.py`, `policy/embeddings.OpenAIEmbedder`), but no live network run is recorded here. |
| Level 2 query/stop policies improve over fixed baselines. | Mechanisms implemented and toggleable (`query_mode`, `stop_mode`); the synthetic demo shows the learned policy stopping early (mean 1 call at budget 1). A dedicated Study 2/3 runner is future work. |

## ⬜ Not yet verified

- Any claim about a **real** LiveBrowseComp or BrowseComp score. None is made.
- Reward-ablation (Study 4) and Thompson-sampling comparisons as committed reports.
- Entity-disjoint splits on real data (time-disjoint mode is implemented).

## 🚫 Unsupported claims to avoid

- "The agent memorizes / recalls facts." — It does not; it learns procedural
  policy. Policy memory is answer-free.
- "Improves on BrowseComp/LiveBrowseComp." — Not demonstrated; only the
  synthetic harness is measured here.
- "Beats hosted web search." — No live comparison is recorded.
- "Improvement holds in-sample, therefore it is real." — OPTIMIZE is never the
  headline; only CONFIRM results are reported.
- "The regimes loop reliably finds promotions." — On this synthetic set the
  baseline policy is already strong, so the loop frequently (and correctly)
  **rejects** updates rather than promoting noise.

## Result artifacts index

- `results/demo/report.json` — per-condition metrics, significance, replay.
- `results/demo/summary.md` — human-readable headline + limitations.
- `results/demo/budget_curve.csv` — `correct_per_tool_call` by budget/condition.
- `results/demo/per_question.csv` — per-item correctness, calls, regime label.
- `results/demo/{tool,query,stop_verify}_rewards.csv` — learned reward tables.
- `results/demo/memory_snapshot.json` — the frozen, answer-free policy memory.
- `results/demo/replay_check.md` — determinism proof.
- `results/regimes-demo/policy_updates.json` — promotion/rejection record.
- `results/demo/debug_questions.jsonl` — bounded, secret-free per-question debug previews (tool sequence, providers, evidence, inferred failure seam, regime names); read by `scripts/debug_run_failures.py` and `scripts/summarize_provider_returns.py`.
- `results/demo/graph_projection.json` — standardized, compact, secret-free typed graph projection of the run (17 object + 14 relation types incl. `eligibility_verdict`, `failure_regime`, `policy_fragment` lineage); provenance-linked to the event-log replay. In the artifact hash ledger.
